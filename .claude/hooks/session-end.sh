#!/bin/bash
# Stop event hook. Sprint logging and memory persistence are enforced by
# .claude/rules/sprint-logging.md loaded into every session via CLAUDE.md —
# no runtime reminder needed here.
exit 0
